Replace methods.json according to the version used by the server's JSONAPI plugin (you can use the same file). Methods.json is simply a file that comes with the BukkitJSONAPI plugin.

1) Compile the solution.

2a) Run bin\\BukkitJSONAPI_GUI.exe
OR
2b) Run bin\\BukkitJSONAPI_Updater.exe